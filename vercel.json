{
  "buildCommand": "pnpm turbo run build --filter=@react-native-vibe-code/web --force",
  "installCommand": "pnpm install",
  "framework": "nextjs",
  "_comment_outputDirectory": "DO NOT add outputDirectory here. Vercel auto-detects the Next.js app in apps/web/ and resolves outputDirectory relative to it, which causes double paths like apps/web/apps/web/.next",
  "crons": [
    {
      "path": "/api/x-bot/poll",
      "schedule": "* * * * *"
    }
  ],
  "headers": [
    {
      "source": "/api/(.*)",
      "headers": [
        {
          "key": "Access-Control-Allow-Origin",
          "value": "*"
        },
        {
          "key": "Access-Control-Allow-Methods",
          "value": "GET, POST, PUT, DELETE, OPTIONS"
        },
        {
          "key": "Access-Control-Allow-Headers",
          "value": "Content-Type, Authorization, X-Requested-With, Cookie, Accept, Accept-Version, Content-Length, Content-MD5, Date, X-Api-Version"
        },
        {
          "key": "Access-Control-Max-Age",
          "value": "86400"
        }
      ]
    }
  ]
}
